package model;

public class Printer {

    // EFFECTS: returns string in form "start message"
    public String print(String start, String message) {
        return (start + " " + message);
    }
}

